services:
  mcp-server:
    build: .
    environment:
      - SUPABASE_PROJECT_REF=host.docker.internal:54322
      - SUPABASE_DB_PASSWORD=postgres
    ports:
      - "5173:5173"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true
